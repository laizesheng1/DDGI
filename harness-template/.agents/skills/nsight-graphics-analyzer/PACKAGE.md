---
name: Nsight Graphics 2026 Skill
description: Drive NVIDIA Nsight Graphics 2026.1+ from the command line for GPU
  performance analysis, frame capture, frame trace inspection, draw-call
  inspection, NVTX/D3DPERF stage timing, replay metadata extraction, and GPU
  Trace drill-downs. Use when the user mentions nsight, ngfx, GPU trace, frame
  capture, frame trace, gpu profiling, GPU performance, or asks why a frame is
  slow on the GPU. Captures frames from a target game (Graphics Capture or GPU
  Trace), auto-exports the GPU Trace TSV bundle, and exposes small JSON queries
  the agent can read cheaply. Wraps `ngfx.exe`, `ngfx-capture.exe`,
  `ngfx-replay.exe`.
version: 1.0.0
---

# Nsight Graphics 2026 Skill

Drive NVIDIA Nsight Graphics 2026.1+ from the command line for GPU performance analysis, frame capture, frame trace inspection, draw-call inspection, NVTX/D3DPERF stage timing, replay metadata extraction, and GPU Trace drill-downs. Use when the user mentions nsight, ngfx, GPU trace, frame capture, frame trace, gpu profiling, GPU performance, or asks why a frame is slow on the GPU. Captures frames from a target game (Graphics Capture or GPU Trace), auto-exports the GPU Trace TSV bundle, and exposes small JSON queries the agent can read cheaply. Wraps `ngfx.exe`, `ngfx-capture.exe`, `ngfx-replay.exe`.
